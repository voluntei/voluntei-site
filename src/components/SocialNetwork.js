import React from 'react';
import Icon from "../assets/facebook.svg";

const SocialNetwork = () => {
    return (
        <Icon className={'hey'} style={{ width: 10 }} />
    );
};

export default SocialNetwork; 